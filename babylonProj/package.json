{
  "name": "testproj",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "meshes01build": "tsc && vite build meshes01",
    "meshes01preview": "vite preview meshes01 --host",
    "collision03build": "tsc && vite build collision03",
    "collision03preview": "vite preview collision03 --host",
    "villagebuild": "tsc && vite build village",
    "villagepreview": "vite preview village --host",
    "player04build": "tsc && vite build player04",
    "player04preview": "vite preview player04 --host",
    "multibuild": "tsc && vite build multi",
    "multipreview": "vite preview multi --host"
    




  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  }
}
